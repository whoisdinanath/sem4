#include <stdio.h>
#include <math.h>

#define MAX 10

int main()
{
  int m_rows, m_cols, i, j, k;
  float ratio, pivot;

  float matrix[m_rows][m_cols];
  printf("Enter the size of matrix: ");
  scanf("%d%d", matrix[])
}
